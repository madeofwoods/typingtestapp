export type DashboardProps = {
  typedChars: string;
  elapsedTime: number;
  errors: number;
  timeRemaining: number;
  accuracy: number;
  speedArray: number[];
  wpm: number;
  counter: number;
};
